#include"main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: first and
 * @b: second input
 * Return: product of a & b
 */

int mul(int a, int b)
{
int product = a * b;

return (product);
}
