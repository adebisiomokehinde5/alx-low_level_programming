#include "main.h"
/**
 * _islower - function used for checking lower cases
 * @c: is the parameter function
 * Return: 0
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')

	{
		return (1);
	}
	else
		return (0);
}
